{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:onboarding": "tsc --project tsconfig.json --outDir onboarding/build",
    "build:ticketing": "tsc --project tsconfig.json --outDir ticketing/build",
    "build:signature": "tsc --project tsconfig.json --outDir signature/build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.7.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
